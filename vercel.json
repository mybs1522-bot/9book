{
    "version": 2,
    "framework": "vite",
    "buildCommand": "chmod -R +x node_modules/.bin && npm run build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "cleanUrls": true
}